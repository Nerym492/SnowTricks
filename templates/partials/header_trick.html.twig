<header class="head-trick-details">
    {% if headerImageExist is not empty %}
        <img src="{{ path(
            'get_trick_image',
            {'trickName': trick.name,'imageName': headerImage}
        ) }}" alt="image {{ trick.name }}">
    {% else %}
        <img src="public/build/images/image-placeholder.jpg" alt="Image placeholder">
    {% endif %}
    <h1>{{ trick.name }}</h1>
</header>